# Cálculo da média de duas notas de um aluno

print('Olá, este programa calcula a média entre duas notas.\n')

n1 = float(input('Digite 1ª nota: '))
n2 = float(input('Digite 2ª nota: '))

print('A média entre essas duas notas é {:.2F}'.format((n1+n2)/2))
